# This is for CoreOS - python installation is not in the default /usr/bin
# Comment this if the tarnet OS os mainstream linux release
ansible_python_interpreter: "${python_interpreter}"

# IP addres of the manager
manager_ip: "${manager_ip}"
